import HashLoader from "react-spinners/HashLoader";

const Loader = () => {
  return(


    <div className="flex justify-center py-40 px-20">
    <HashLoader color="#1E40AF" size={50} />
  </div>
  )
};

export default Loader;
